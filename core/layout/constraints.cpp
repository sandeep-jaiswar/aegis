#include "core/layout/constraints.hpp"

// This file exists to ensure the constraints module can be compiled independently
// All functionality is header-only (constexpr), but CMake expects a .cpp file

namespace aegis::core::layout {
// No implementation needed - all constexpr
} // namespace aegis::core::layout
