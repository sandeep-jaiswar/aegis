#include "core/layout/layout_box.hpp"

// This file exists to ensure the layout_box module can be compiled independently
// All functionality is header-only, but CMake expects a .cpp file

namespace aegis::core::layout {
// No implementation needed - all types are POD
} // namespace aegis::core::layout
